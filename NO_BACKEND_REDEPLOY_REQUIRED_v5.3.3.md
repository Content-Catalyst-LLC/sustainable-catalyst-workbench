# No backend redeploy required — Workbench v5.3.3

Workbench v5.3.3 is a WordPress/browser integration-hardening release. The certified FastAPI runtime remains v5.3.0 and its `/v510`, `/v520`, and `/v530` routes are unchanged.

Update the WordPress plugin only. Do not rebuild the Contabo `sc-workbench` container for this release.
