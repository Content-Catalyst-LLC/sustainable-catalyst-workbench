# Sustainable Catalyst Workbench v0.9.1 — Equation and Article-Aware Layer

# Push the repo package to GitHub
cd ~/Downloads
chmod +x update_and_push_sustainable_catalyst_workbench_v0_9_1_to_github.sh
./update_and_push_sustainable_catalyst_workbench_v0_9_1_to_github.sh

# Render backend settings remain the same as v0.8.x.
# After deployment, test:
curl https://YOUR-RENDER-URL.onrender.com/health
curl -X POST https://YOUR-RENDER-URL.onrender.com/equations/analyze \
  -H 'Content-Type: application/json' \
  -d '{"equation":"S_{t+1}=S_t+I_t-O_t","context":"stock flow limits to growth","mode":"analyst"}'

# WordPress admin
# SC Workbench → Equation Registry → Scan / Rebuild Equation Registry

# Recommended shortcode for article-aware use:
# [sc_workbench mode="auto"]
