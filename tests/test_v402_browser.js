const fs=require('fs');const path=require('path');const vm=require('vm');const assert=require('assert');
const storage={};const localStorage={get length(){return Object.keys(storage).length;},key(i){return Object.keys(storage)[i]||null;},getItem(k){return Object.prototype.hasOwnProperty.call(storage,k)?storage[k]:null;},setItem(k,v){storage[k]=String(v);}};
const document={readyState:'complete',documentElement:{},querySelector(){return null;},querySelectorAll(){return[];},addEventListener(){},dispatchEvent(){}};const windowObject={setTimeout,clearTimeout,addEventListener(){}};
const context={window:windowObject,document,localStorage,console,Promise,Date,JSON,Array,String,Number,Object,Math,Error,CustomEvent:function(type,options){this.type=type;this.detail=options&&options.detail;},MutationObserver:undefined};Object.assign(windowObject,{window:windowObject,document,localStorage});
const source=fs.readFileSync(path.join(__dirname,'..','wordpress-plugin','sustainable-catalyst-workbench','assets','js','sc-workbench-v402.js'),'utf8');
for(const marker of ["var VERSION = '4.0.2'",'threeWayReconciliation: true','automaticExecutionAuthorized: false','automaticRollbackAuthorized: false','automaticDeletionAuthorized: false','ROLLBACK CONNECTED PROJECT'])assert(source.toLowerCase().includes(marker.toLowerCase()),`Missing marker: ${marker}`);
vm.runInNewContext(source,context,{filename:'sc-workbench-v402.js'});const api=windowObject.SCWBGraphSyncRecovery;assert(api,'Graph/sync recovery API not exported');assert.strictEqual(api.version,'4.0.2');assert.strictEqual(api.automaticExecutionAuthorized,false);assert.strictEqual(api.automaticRollbackAuthorized,false);assert.strictEqual(api.automaticDeletionAuthorized,false);assert.strictEqual(api.graphAudit().expectedStudioCount,22);assert(api.receipt().requiredFields.includes('journalHash'));console.log('Workbench v4.0.2 browser graph, synchronization, and recovery regression passed.');
