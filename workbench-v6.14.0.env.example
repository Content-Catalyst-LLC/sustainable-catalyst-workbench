# Sustainable Catalyst Workbench v6.14.0
SCWB_ALLOWED_ORIGINS=
SCWB_CORE_URL=
SCWB_CORE_API_KEY=
SCWB_CORE_ENABLED=false
SCWB_CORE_REQUIRED=false
SCWB_CORE_EXPECTED_VERSION_PREFIX=3.
SCWB_SERVICE_TOKEN=
SCWB_REQUIRE_SERVICE_TOKEN=false
# v6.12 captures declared Workbench research state and prepares explicit Core
# project-state, reproducible-package, context, reconstruction, and snapshot plans.
# It never restores state, replays executions, dispatches to Core, or certifies reproducibility automatically.

# v6.14 prepares Platform Core integration-certification evidence and registry requests.
# Certification is runtime-contract conformance only; no automatic dispatch, product authorization,
# scientific-validity certification, product ranking, inferred reproducibility, or truth determination.
